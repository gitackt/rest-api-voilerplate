overwrite: true
schema: './src/api/graphql/schema.graphql'
documents: null
generates:
  src/api/graphql/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
  ./graphql.schema.json:
    plugins:
      - 'introspection'
