type Query {
  users: [User!]
  user(id: ID!): User!
}

type Mutation {
  createUser(name: String!, age: Int!): User
}

type User {
  id: ID!
  name: String!
  age: Int!
}

type Post {
  id: ID!
  title: String!
  content: String!
}
